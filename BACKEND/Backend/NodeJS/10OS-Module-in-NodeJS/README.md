# OS Module in NodeJS Methods and Properties 🚀❤️‍🔥

👉 `os.platform()`

👉 `os.arch()`

👉 `os.userInfo()`

👉 `os.freemem()`

👉 `os.totalmem()`

👉 `os.uptime()`

👉 `os.homedir()`

👉 `os.hostname()`

👉 `os.networkInterfaces()`

👉 `os.cpus()`

👉 `os.tmpdir()`

👉 `os.type()`
